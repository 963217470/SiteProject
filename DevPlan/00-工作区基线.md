# Batch 0：工作区基线

## STR-001 固化目标目录

- [x] DONE
- Verified: `npm run build`（连续两次）；目录职责见 `docs/architecture/README.md`。
- 范围：确认 `theme/lib`、`theme/services`、`theme/types`、领域组件、`supabase/migrations`、`supabase/functions`、`supabase/tests` 和根级 `tests` 的职责。
- 验收：目录说明与实际代码放置规则一致；不移动仍被运行代码引用的文件。
- 验证：`npm run build`。

## STR-002 清理生成物跟踪

- [x] DONE
- Verified: `git ls-files docs/.vitepress/cache docs/.vitepress/dist` 无输出；连续两次构建后生成物未重新进入跟踪。
- 范围：从 Git 跟踪中移除 `docs/.vitepress/cache` 与 `docs/.vitepress/dist`，保留本地生成能力。
- 验收：生成物不再出现在源码 diff 中，CI 仍从源码构建。
- 验证：构建两次后 `git status` 不出现生成物。

## STR-003 建立环境变量边界

- [x] DONE
- Verified: `npm run check:secrets`；`.env.example` 已区分浏览器公开变量与服务端秘密。
- 范围：只在 `.env.example` 保留变量名和安全示例，区分浏览器变量与服务端秘密。
- 验收：浏览器变量不包含 `service_role`、GitHub 写入 token 或其他特权凭据。
- 验证：仓库秘密扫描无命中。

## STR-004 建立架构决策记录

- [x] DONE
- Verified: 已建立 ADR 模板，并由 ADR-0001 记录数据主源、认证、托管平台和特权操作边界。
- 范围：创建 ADR 模板，记录数据主源、认证方式、托管平台和特权操作边界。
- 验收：后续重大架构选择可追踪，不再只存在于聊天或过期方案文档中。
- 验证：至少补齐当前四项架构决定。
