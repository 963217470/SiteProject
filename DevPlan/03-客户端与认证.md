# Batch 3：客户端与认证

## APP-001 建立 Supabase 单例

- [x] DONE
- Verified: `npm run build` 与秘密扫描通过；VitePress 不再加载 `supabase-rest.js` 或内联公开配置；本地 `/login/` 页面加载成功且浏览器控制台无错误，缺少环境变量时不创建客户端。
- 范围：在 `theme/lib/supabase.ts` 创建唯一官方 SDK 客户端。
- 验收：URL 和 anon key 来自环境变量；不再动态加载远程 SDK。
- 验证：构建及登录页加载成功。

## APP-002 移除自制 REST 客户端

- [x] DONE
- Verified: 源码中不存在 `window.__supabase` 或自制 `QueryBuilder`；`docs/public/supabase-rest.js` 已删除，页面统一取得官方 SDK 单例；秘密扫描、安全检查和生产构建通过。额外本地浏览器复验被浏览器安全策略拒绝访问该地址。
- 范围：将 `window.__supabase` 调用逐步迁移到官方 SDK，最终删除兼容层。
- 验收：项目只有一套查询、认证和 Storage 行为。
- 验证：全仓库不存在 `window.__supabase` 和自制 QueryBuilder 调用。

## APP-003 统一认证状态

- [ ] IN PROGRESS
- Implemented: 已建立模块级 `useAuth`，统一 session、user、profile、loading、error、可重试初始化、GitHub 登录、退出及单例 Auth 订阅；导航、登录页和回调页已迁移，旧的手工 token 解析与存储已删除。
- Verified: `npm run build`、`npm run check:secrets`、`npm run check:security` 通过；VitePress 从仓库根目录读取本地环境变量，本地 `/login/` 已加载 Supabase 配置，未配置状态和 `/auth/callback` 失败状态显示正确，浏览器控制台无错误。
- Blocked: 当前环境没有 GitHub OAuth 测试凭据，尚未完成真实登录、刷新页面、token 过期和已登录退出的运行时验收。
- 范围：建立 `useAuth`，统一初始化、刷新、登录、回调、退出和订阅清理。
- 验收：任意页面获得一致的 user、profile、loading 和 error 状态。
- 验证：登录、刷新页面、token 过期、退出流程。

## APP-004 统一权限派生

- [ ] IN PROGRESS
- Implemented: 已建立统一角色规范化、标签、说明与 `usePermissions` 派生；导航、个人页、内部资源页和后台入口已迁移，未知/缺失角色默认降级为 `user`，不再冒充社员。
- Verified: 生产构建、秘密扫描和安全基线通过；匿名用户访问个人页显示登录提示，访问内部资源显示拒绝状态，浏览器控制台无页面错误；数据库 8 组角色/RLS 事务测试通过。
- Blocked: 仍需准备 user/member/admin 三角色测试账号，完成导航、内部资源和后台入口的浏览器矩阵后才能标记 DONE。
- 范围：建立 `usePermissions`，只负责界面可见性与操作提示。
- 验收：角色判断不再散落在页面脚本中；数据库仍是最终授权者。
- 验证：各角色界面与 RLS 结果一致。

## APP-005 建立统一错误模型

- [x] DONE
- Implemented: 已建立稳定的 auth/network/forbidden/validation/config/not_found/unknown 错误模型和安全用户文案；共享认证、登录、OAuth 回调和内部资源页已迁移，不再直接展示底层 Supabase 错误。
- Verified: 全部产品页面不再拼接底层 `.message`（专用调试页除外）；浏览器验证文章 loading/empty、OAuth error、内部资源 forbidden 状态均可触发且控制台无页面错误；秘密扫描、安全基线和生产构建通过。
- 范围：将认证、网络、权限、校验和未知错误映射为稳定错误类型。
- 验收：组件不再直接拼接底层错误文本。
- 验证：loading、empty、error、forbidden 状态均可触发。

## APP-006 完成 OAuth 回跳保护

- [x] DONE
- Implemented: 已集中校验登录后回跳，只接受同源绝对路径，拒绝外部/协议相对 URL、反斜杠、控制字符和认证循环路径；回调统一清理一次性跳转状态并处理 GitHub 授权失败。
- Verified: `npm run test:auth-redirect` 覆盖合法路径、外部 URL、协议相对 URL、反斜杠、控制字符、认证循环与空值；秘密扫描、安全基线和生产构建通过。额外浏览器失败回调复验被浏览器安全策略拒绝访问新本地端口。
- 范围：限制登录后跳转为同源相对路径，统一处理失败和过期回调。
- 验收：不能利用 redirect 参数跳转到外部恶意站点。
- 验证：合法路径、外部 URL、畸形参数测试。
