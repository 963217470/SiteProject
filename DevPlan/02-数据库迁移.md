# Batch 2：数据库迁移

## DB-001 盘点生产 Schema

- [ ] TODO
- 范围：导出当前表、列、约束、索引、函数、触发器、GRANT、RLS 与 Storage 配置。
- 验收：形成可审查的当前状态快照，不包含真实用户数据。
- 依赖：需要生产数据库只读权限。
- 验证：快照覆盖前端实际查询的全部表和字段。

## DB-002 创建 profiles 基线迁移

- [x] DONE
- Verified: 本地 Supabase 执行 `db reset --version 202607100000 --no-seed` 成功；`profiles_baseline.sql` 的新用户、本人和管理员权限断言通过；`npm run check:secrets` 与 `npm run build` 通过。
- 范围：用户资料、角色约束、新用户触发器、索引和 RLS。
- 验收：空数据库执行后可安全创建和读取 profile。
- 验证：新用户、本人、管理员权限测试。

## DB-003 创建 articles 基线迁移

- [x] DONE
- Verified: 本地 Supabase 从零执行至 `202607100002` 成功；profiles 与 articles 事务测试均通过；`npm run check:secrets`、`npm run check:security`、`npm run build` 均通过。
- 范围：文章完整表结构、状态、可见性、作者关系、审核字段、知识库字段前置约束。
- 验收：不依赖生产项目既有表即可创建 articles。
- 验证：空数据库迁移和文章状态权限测试。

## DB-004 创建互动迁移

- [x] DONE
- Verified: 本地 Supabase 从零执行至 `202607100003` 成功；profiles、articles、interactions 三组事务测试通过，重复写入仅保留一条记录，跨用户写入/修改/删除被拒绝；秘密扫描、安全检查和生产构建通过。
- 范围：点赞、收藏、评论表及索引、唯一约束和 RLS。
- 验收：`(article_id, user_id)` 无法重复点赞或收藏。
- 验证：并发重复写入只保留一条有效记录。

## DB-005 原子维护互动计数

- [x] DONE
- Verified: 本地 Supabase 从零执行至 `202607100004` 成功；四组数据库事务测试通过，互动增删后计数与明细一致，直接覆盖计数被拒绝；前端不再写入计数字段；秘密扫描、安全检查和生产构建通过。
- 范围：使用触发器、聚合视图或原子 RPC 维护点赞数和评论数。
- 验收：浏览器不再直接覆盖计数字段。
- 验证：并发增删后计数与明细一致。

## DB-006 创建知识库迁移

- [x] DONE
- Verified: 本地 Supabase 从零执行至 `202607100005` 成功；截至知识库的全部事务测试通过，普通用户仅能申请自己的文章，管理员可建立分支并审批，跨用户读取/申请被拒绝；秘密扫描、安全检查和生产构建通过。
- 范围：知识分支、分支申请、文章关联、索引和 RLS。
- 验收：普通用户只能创建自己的申请，管理员才能审批和维护分支。
- 验证：申请、审批、拒绝和跨用户访问测试。

## DB-007 创建内部资源迁移

- [x] DONE
- Verified: 本地 Supabase 完整迁移链从空库执行成功；资源元数据与 Storage 组合测试覆盖普通用户拒绝、社员读取、管理员上传/管理、私有 bucket 和 500 MiB 边界；安全加固事务测试、秘密扫描、安全检查和生产构建通过。
- 范围：资源元数据、私有 bucket、Storage RLS、文件大小与类型边界。
- 验收：社员可读、管理员可管理、其他身份拒绝。
- 验证：元数据和文件访问权限组合测试。

## DB-008 创建资料审核迁移

- [x] DONE
- Verified: 本地 Supabase 完整迁移成功；资料审核事务测试覆盖本人提交、跨用户拒绝、管理员原子批准/拒绝及重复审批拒绝；安全加固回归、秘密扫描、安全检查和生产构建通过。
- 范围：资料变更申请、审核状态、管理员审批操作和 RLS。
- 验收：用户不能绕过审核直接修改公开资料。
- 验证：提交、批准、拒绝和重复审批测试。

## DB-009 建立数据库引导入口

- [x] DONE
- Verified: `npm run test:db` 可从空库执行全部迁移并依次通过 8 个数据库测试文件；管理员初始化、可选 seed、生产迁移边界和停止/启动命令已记录；秘密扫描、安全检查和生产构建通过。
- 范围：说明迁移顺序、初始化管理员方式和可选种子数据。
- 验收：新环境可以从零重建，不需要手工猜测 SQL 顺序。
- 验证：在临时 Supabase 环境完整执行一次。

## DB-010 生成 TypeScript 数据库类型

- [ ] TODO
- 范围：生成并提交数据库类型，供 service 层使用。
- 验收：查询字段和写入结构具有静态类型。
- 验证：`typecheck` 通过。
