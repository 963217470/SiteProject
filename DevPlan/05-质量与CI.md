# Batch 5：质量与 CI

## QA-001 增加 typecheck

- [ ] TODO
- 范围：配置 Vue/TypeScript 静态检查和 npm script。
- 验收：类型错误导致本地检查和 CI 失败。
- 验证：`npm run typecheck`。

## QA-002 增加 lint

- [ ] TODO
- 范围：配置适合 Vue、TypeScript 和浏览器代码的最小 lint 规则。
- 验收：不引入大规模纯格式化 diff。
- 验证：`npm run lint`。

## QA-003 增加单元测试

- [ ] TODO
- 范围：优先覆盖权限派生、数据转换、Markdown 安全处理和状态转换。
- 验收：核心纯函数有稳定测试。
- 验证：`npm run test`。

## QA-004 增加权限集成测试

- [ ] TODO
- 范围：覆盖游客、普通用户、社员、管理员的 RLS 允许与拒绝路径。
- 验收：每项关键权限至少有一条拒绝测试。
- 验证：`npm run test:permissions`。

## QA-005 增加 CI 门禁

- [ ] TODO
- 范围：依次运行安装、秘密扫描、类型检查、lint、测试和构建。
- 验收：任何步骤失败均阻止部署。
- 验证：在测试分支分别制造并恢复一次可控失败。

## QA-006 固定 Node 版本

- [ ] TODO
- 范围：使本地说明、版本文件和 GitHub Actions 使用同一 Node 主版本。
- 验收：本地与 CI 构建环境一致。
- 验证：全新环境按说明完成安装和构建。

## QA-007 建立依赖安全处理流程

- [ ] TODO
- 范围：审查当前 npm audit 告警，区分可直接升级与破坏性升级。
- 验收：高危问题有修复或明确接受记录。
- 验证：重新运行依赖审计。
