# Batch 1：安全整改

## SEC-001 轮换泄露的 service role

- [ ] TODO
- Blocked: 当前环境没有 Supabase 控制台/Management API 授权；不得仅凭源码删除将本项标记完成。
- 范围：在 Supabase 生产项目轮换已暴露的 `service_role` 密钥。
- 验收：仓库中的旧密钥无法再访问生产数据。
- 依赖：需要生产环境授权。
- 验证：旧密钥请求被拒绝，新密钥仅保存在服务端秘密存储。

## SEC-002 删除公开特权密钥

- [x] DONE
- Verified: `npm run check:secrets`、`npm run build`、`npm run check:secrets:build` 均通过。
- 范围：删除源码、测试页和构建产物中的 `service_role`，移除公开 API 测试页。
- 验收：前端包和仓库文本中不存在该密钥或等价特权 token。
- 验证：秘密扫描和生产构建内容扫描。

## SEC-003 禁止浏览器使用 service role

- [ ] IN PROGRESS
- Implemented: 已删除全部 `.useServiceRole()`，浏览器读写和上传仅使用公开 key 与当前用户 JWT。
- Blocked: 等待迁移应用后执行匿名、普通用户、社员、管理员运行时验证。
- 范围：删除 `.useServiceRole()` 和所有以 service role 执行的浏览器读写、上传逻辑。
- 验收：浏览器只使用 `anon` key 与当前用户 JWT。
- 验证：匿名、普通用户、社员和管理员分别验证允许与拒绝路径。

## SEC-004 修复用户角色越权

- [ ] IN PROGRESS
- Implemented: 迁移增加角色变更触发器与管理员策略，拒绝测试已写入 `supabase/tests/security_hardening.sql`。
- Blocked: 当前没有可用 Supabase/Postgres 测试实例，尚未执行数据库拒绝测试。
- 范围：禁止普通用户通过更新自己的 profile 修改 `role`。
- 验收：角色只能由管理员受控操作修改。
- 验证：普通用户直接调用 REST 更新 `role` 返回拒绝。

## SEC-005 收紧文章写入策略

- [ ] IN PROGRESS
- Implemented: INSERT 强制当前作者且状态仅允许 `draft/pending`，并补齐匿名、冒充作者、自行发布拒绝测试。
- Blocked: 当前没有可用 Supabase/Postgres 测试实例，尚未执行数据库拒绝测试。
- 范围：文章 INSERT 必须登录且 `author_id = auth.uid()`；普通用户只能创建 `draft` 或 `pending`。
- 验收：匿名提交、冒充作者、自行发布均被数据库拒绝。
- 验证：为三种拒绝场景增加权限测试。

## SEC-006 补齐互动表 RLS

- [ ] IN PROGRESS
- Implemented: 点赞、评论、收藏均已启用 RLS，并补齐跨用户写入与删除拒绝测试。
- Blocked: 当前没有可用 Supabase/Postgres 测试实例，尚未执行数据库拒绝测试。
- 范围：为点赞、评论、收藏启用 RLS，并限定用户只能操作自己的记录。
- 验收：无法替其他用户点赞、收藏或评论。
- 验证：跨用户写入和删除均被拒绝。

## SEC-007 将内部资源改为私有

- [ ] IN PROGRESS
- Implemented: resources bucket 设为私有，下载改为 60 秒 Signed URL，存储策略按社员/管理员限制。
- Blocked: 当前没有可用 Supabase 项目连接，尚未执行四身份下载验证。
- 范围：将 resources bucket 改为私有，使用受控下载或短期 Signed URL。
- 验收：未登录和非社员无法通过固定 URL 下载资源。
- 验证：游客、普通用户、社员、管理员四种身份测试。

## SEC-008 增加秘密扫描

- [x] DONE
- Verified: CI 已扫描源码和生产构建；无效 GitHub token 样例会使检查失败，样例已删除。
- 范围：在本地检查和 CI 中扫描 JWT、Supabase service key、GitHub token 和 `.env`。
- 验收：新增类似秘密时 CI 失败。
- 验证：使用无效测试样例确认规则可触发，再删除样例。
