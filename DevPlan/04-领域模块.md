# Batch 4：领域模块

## MOD-001 抽取 profiles service

- [x] DONE
- Implemented: 已建立类型化 profiles service，集中本人/公开资料、批量资料、待审核申请、资料申请提交和管理员审核 RPC；统一认证与个人页已迁移。
- Verified: 除专用 debug 页面外，产品页面不再直接查询 `profiles/profile_changes` 或调用审核 RPC；数据库 profiles 与 profile-review 事务测试覆盖本人资料、公开资料、审核中状态、跨用户拒绝及管理员审批；`npm run typecheck`、秘密扫描、安全基线和生产构建通过。
- 范围：集中用户资料、资料申请和公开资料查询。
- 验收：profile 页面不直接拼 Supabase 查询。
- 验证：本人资料、公开资料、审核中状态。

## MOD-002 抽取 articles service

- [x] DONE
- Implemented: 已建立类型化 articles service，集中公开/内部/作者/管理/知识库列表、详情、批量读取、创建、更新、删除及草稿/提交/发布/拒绝状态转换；所有产品页面的 articles 表访问和文章状态转换均已迁移到统一 service。
- Verified: 产品源码不再直接查询 `articles` 表；数据库 article-write 事务测试已覆盖草稿、待审核、发布、拒绝及越权拒绝路径；`npm run typecheck`、秘密扫描、安全基线和生产构建通过。
- 范围：集中草稿、提交、列表、详情、更新和删除操作。
- 验收：文章状态转换入口统一。
- 验证：草稿、待审核、发布、拒绝流程。

## MOD-003 拆分文章编辑器

- [x] DONE
- Implemented: 将编辑器元数据解析、图片与附件路径处理、知识库树整理、文章校验与保存流程拆入 `article-editor/` 独立模块；顶层 `ArticleEditor` 仅装配页面状态、上传事件和保存动作。
- Verified: 新增文章编辑器测试，覆盖必填校验、知识库目标校验、摘要生成，以及新建草稿和提交审核两条保存路径；`npm run typecheck`、秘密扫描、安全基线和生产构建通过。
- 范围：将元数据、正文编辑、图片、知识库选项和保存流程拆成独立组件/组合函数。
- 验收：顶层编辑器只负责编排，不包含全部业务实现。
- 验证：新建草稿和提交审核。

## MOD-004 抽取 interactions service

- [ ] TODO
- 范围：集中点赞、收藏、评论查询和操作。
- 验收：客户端不再手工维护权威计数。
- 验证：重复操作、并发操作和未登录操作。

## MOD-005 抽取 knowledge-base service

- [ ] TODO
- 范围：集中分支树、文章关联、分支申请和管理员维护。
- 验收：知识库页面和管理页共享同一数据规则。
- 验证：空树、嵌套树、申请与审批。

## MOD-006 抽取 resources service

- [ ] TODO
- 范围：集中内部资源列表、上传、签名下载和删除。
- 验收：组件不保存永久公开文件地址。
- 验证：上传限制、权限拒绝、签名 URL 过期。

## MOD-007 将文章列表页组件化

- [ ] TODO
- 范围：把 Markdown 内联脚本迁移为 Vue 组件和 composable。
- 验收：页面 Markdown 只保留 frontmatter 与组件挂载。
- 验证：筛选、排序、空状态和错误状态。

## MOD-008 将文章详情页组件化

- [ ] TODO
- 范围：拆分正文渲染、作者、互动、评论和下载区域。
- 验收：详情页不再维护超长全局脚本。
- 验证：公开、内部、未登录和不存在文章。

## MOD-009 将管理后台组件化

- [ ] TODO
- 范围：抽取文章审核、资料审核、知识库管理的共享守卫和状态组件。
- 验收：管理页不重复实现 `requireAdmin` 与数据等待逻辑。
- 验证：管理员与非管理员访问。

## MOD-010 建立最小占位界面规范

- [ ] TODO
- 范围：提供通用 Loading、Empty、Error、Forbidden、Confirm 组件。
- 验收：只使用默认主题和语义化结构，不进行正式视觉设计。
- 验证：键盘可操作、状态文案清楚、构建通过。
