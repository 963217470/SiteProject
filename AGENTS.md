# AGENTS.md

本文件约束在本仓库工作的所有 AI agent。目标是：agent 能自主完成从需求澄清、实现到验证和交付的全部工作，同时让不同职能的责任清楚、结果可检查。

## 1. 工作原则

- 对交付结果负责，不只给建议、方案或半成品。除非用户只要求分析，否则应完成实现、验证及必要文档更新。
- 先阅读相关代码和文档，再修改；以用户当前要求和仓库实际行为为准。文档与代码冲突时，指出冲突并同步修正受影响内容。
- 优先做满足需求的最小完整改动，沿用现有技术栈与模式；不要顺手重构无关代码或引入不必要依赖。
- 可逆、仓库内、低风险的决定由 agent 自主做出。只有缺少关键业务选择、需要凭据、涉及生产数据/部署/付费或其他不可逆外部操作时才询问用户。
- 发现阻塞时先自行排查并尝试安全替代方案；最终交付必须明确说明已完成内容、验证结果和仍存在的限制。

## 2. 职能分离

以下是责任帽子，不是强制的人员数量。简单任务可由一个 agent 依次承担全部职能；复杂且可独立并行的任务可委派给多个 agent。无论如何，必须有一个任务负责人统一范围、整合结果并做最终验收，不能把集成责任留给用户。

### 任务负责人

- 明确目标、验收标准、影响范围与实施顺序。
- 分配或兼任其他职能，处理跨模块取舍。
- 检查最终 diff、验证结果以及文档一致性。

### 产品与内容

- 从 `方案设计.md` 和 `docs/01-项目概述.md` 至 `docs/08-前端页面设计.md` 提取业务规则。
- 保证页面信息、中文文案、导航和空状态真实、完整且一致。
- 需求含糊时采用最保守、最容易撤销的合理解释，并在交付中说明重要假设。

### 体验与前端

- 负责 VitePress、Vue、Markdown 页面、主题、样式、响应式与可访问性。
- 延续现有视觉语言；新增页面必须同时考虑加载、空数据、错误、未登录和无权限状态。
- 交互或视觉改动应在可运行页面中检查，不能只依据源码判断。

### 数据与安全

- 负责 Supabase 客户端、SQL、认证、角色和 RLS 策略。
- 权限必须在数据层强制执行，不能只隐藏前端入口。
- 不提交密钥、token、真实用户数据或 `.env`；新增环境变量时同步更新 `.env.example`。
- SQL 变更应尽量可重复执行，并说明迁移顺序及对现有数据的影响。

### 验证与发布

- 根据改动风险验证主流程、失败流程、权限边界和构建结果。
- 不得为了让检查通过而删除测试、放宽权限或掩盖错误。
- 未经用户明确要求，不执行部署、推送、合并、生产数据库变更等外部操作。

## 3. 仓库约定

- 技术栈：VitePress 1、Vue 3、Supabase、GitHub Pages。
- 后续开发任务以 `DevPlan/README.md` 为入口；领取任务时更新对应状态，完成并验证后才能标记为 `DONE`。
- 站点源码位于 `docs/`；主题与组件位于 `docs/.vitepress/theme/`；Supabase SQL 位于 `supabase/`。
- 新的数据访问代码放入 `docs/.vitepress/theme/services/`，共享客户端放入 `lib/`，数据库类型放入 `types/`；Markdown 页面应保持为薄路由层。
- `supabase/` 根部 SQL 是待迁移的历史脚本；新的数据库基线和变更应进入 `supabase/migrations/`，并在测试通过前保留旧脚本兼容性。
- `docs/.vitepress/dist/` 和 `docs/.vitepress/cache/` 是生成物，不直接编辑，也不把生成物变化当作源码修复。
- 同一能力若同时存在 `.js` 与 `.ts` 实现，先确认实际引用路径，避免只修改未生效的副本。
- 保持现有中文内容与 UTF-8 编码；不要用乱码覆盖文件。
- 新依赖必须确有必要，并同步更新 lockfile。

## 4. 默认工作流

1. 检查 `git status`，阅读需求涉及的源码、设计文档和配置，保留用户已有改动。
2. 若任务来自 `DevPlan/`，先将对应任务改为 `IN PROGRESS`；否则写出简短的内部实施计划和可验证的完成条件。
3. 完成端到端改动：代码、数据策略、配置、文案和相关文档缺一不可。
4. 自查 diff，确认没有无关改动、秘密信息、生成物或明显的权限回归。
5. 运行与改动相称的验证。最低要求是 `npm run build`；UI 改动还需检查相关页面和关键视口；认证/权限改动还需检查不同角色及拒绝路径。
6. 验证通过后将对应 DevPlan 任务改为 `DONE` 并记录验证；交付时简洁报告完成内容、验证结果、假设和未验证风险。
7. 每个 Task 验证完成后，只暂存该 Task 的相关改动并创建一个本地 Git commit；提交信息必须包含 Task ID。除非用户明确要求，不得 push、创建 PR 或执行其他远程发布操作。

## 5. 完成定义

任务只有在以下条件满足时才算完成：

- 用户可见需求已经实际实现，关键失败状态有合理处理。
- 相关角色与权限边界没有仅依赖前端控制。
- 构建或适当检查通过；若无法运行，已说明具体原因和替代验证。
- 相关文档、示例环境变量及配置与实现一致。
- 工作区中没有本任务产生的调试残留、敏感信息或误改生成物。
- 已为该 Task 创建独立的本地 Git commit；存在生产权限等外部阻塞时可以提交已实现部分，但不得把 Task 标记为 `DONE`。
